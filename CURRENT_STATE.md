# 現在の実装状況監査

> **履歴資料：** この文書はコミット `b344334` の監査記録であり、その後の実装状態を表しません。Codex依頼01の変更結果は `TASK_01_RESULT.md` を参照してください。

監査日：2026-09-09  
対象コミット：`b344334`（監査開始時のHEAD）  
対象範囲：リポジトリ内のソース、同梱データ、テスト、ビルド成果物、GitHub Pages workflow

## 1. 結論

現在のリポジトリは、**地図・表示切替・州詳細の枠・議席シミュレーション・GitHub Pages配備の技術的な骨格**までは実装されている。一方、選挙情報アプリとして必要な現職名、候補者、特別選挙、情勢評価、州別解説、出来事は収録されていない。したがって、現状は「完成した選挙情報サイト」ではなく、**未確認データを明示するUIプロトタイプ兼データ構造の初期実装**である。

特に重要な点は次のとおり。

- `states` は50州、`seats` は100議席、`elections` はClass IIから機械生成された通常選挙33件を持つ。
- 特別選挙レコードは0件である。依頼で確認対象とされた「特別選挙2議席」は、州、Class、対象任期、候補者、出典のいずれもリポジトリに存在しない。
- 全100議席の `incumbent` が `null` で、全33選挙の候補者名も `null` である。さらに候補者配列を画面へ描画する処理自体がない。
- 党派だけは50州の圧縮された行データに入っているが、各議席の党派・会派に対応する出典は十分に紐付いておらず、公開前の再確認がREADMEで明記されている。
- シミュレーションは候補者ではなく、改選対象の各 `seatId` に対して仮の当選会派を選ぶ方式である。現状では通常選挙33議席だけが変更対象で、特別選挙2議席は計算に含まれない。

## 2. 実装済み機能

### 2.1 地図と表示

- `us-atlas` の `states-10m.json` を同梱し、D3の `geoAlbersUsa` とTopoJSON変換でSVG州境を描画する。
- FIPSを2桁文字列へ正規化し、州データと地図形状を結合する。
- 「現保有党」と「選挙情勢」の2モードを切り替えられる。
- 「激戦のみ強調」のチェックボックスがあり、`Toss Up`、`Lean D`、`Lean R` 以外の州を薄くする。
- 州のクリック、Enter/Spaceキー、50州のセレクトメニューから州詳細を開ける。
- 選択州の枠、凡例、地図読込失敗時のエラー表示を持つ。
- Alaska/Hawaiiは `geoAlbersUsa` の配置を利用し、D.C.は50州データにないため除外される。

### 2.2 州詳細

- 州名、日本語名・英語名・略称、原稿ステータス、基準日を表示する。
- 対象選挙があればClass・通常選挙・情勢取得状態を表示し、なければ「2026年の対象選挙なし」と表示する。
- 4区分の州要約、年表欄、2議席のClass・党派・会派・次回通常改選年、出典欄の表示枠がある。
- 詳細を閉じるボタンがある。

### 2.3 サマリーとシミュレーション

- 100議席の `caucus` から現在構成を都度集計し、固定の集計値を別に持たない。
- 全選挙レコードから改選対象 `seatId` を作り、各対象議席について民主党会派、共和党会派、未確定を選べる。
- 初期状態へのリセット、変更した議席一覧、仮定後の会派別合計、多数派メッセージを表示する。
- 地図の激戦フィルターはシミュレーションの集計対象を変えない。

### 2.4 アクセシビリティ／レスポンシブ対応

- スキップリンク、州パスのキーボード操作、可視フォーカス、テキスト付き凡例がある。
- ボタン・selectは44 CSS pxを最小高さの目標としている。
- 800px以下と420px以下のレイアウト規則、`prefers-reduced-motion` がある。

## 3. 使用データ

### 3.1 `states`

- 50件。
- FIPS、略称、日本語州名、英語州名、その州の2議席のClassを保持する。
- Class件数はClass Iが33、Class IIが33、Class IIIが34となる。

### 3.2 `seats`

- `states` の圧縮行データから2件ずつ生成され、合計100件。
- `seatId` は `州略称-Class`（例：`AL-2`）形式。
- 圧縮行に記録された党派内訳は R 53、D 45、I 2。
- 会派は `party === 'R'` なら `Republican`、それ以外はすべて `Democratic` として自動生成されるため、表示・集計上は共和党会派53、民主党会派47となる。
- 全議席で `incumbent: null`、`vacant: false`。
- Class IIだけ任期が `2021-01-03`〜`2027-01-03`、Class I/IIIの任期は文字列「未確認」。
- 全議席の `sourceIds` は `senate-class` のみで、党派・会派・空席・任期それぞれに対応する個別出典はない。

### 3.3 `elections`

- `senateClass === 2` の議席からのみ自動生成された33件。
- 全件が `type: 'regular'`、投票日は `2026-11-03`、対象任期は `2027-01-03〜2033-01-03`。
- 各選挙に候補者プレースホルダーが1件あるが、`name: null`、`party: 'unknown'`、`status: 'unconfirmed'`、`caucusIntent: 'unconfirmed'`。
- 全情勢評価が `unavailable` で、原表記、機関名、評価日、取得日、出典はすべて `null` または空配列。
- 特別選挙は0件。

### 3.4 `profiles` / `events` / `sources`

- `profiles` は50州分あるが、すべて `contentStatus: '未作成'`。州名以外は共通の欠測説明で、確認済みの州固有解説ではない。
- `eventIds` は全州で空、`events` 配列も0件。
- `sources` は6件で、U.S. Senate、Census、BLS、BEA、us-atlasへの入口を持つ。ただし州別解説・数値・出来事に使った個別表や記事は登録されていない。
- `retrievedAt: '2026-09-09'` が設定されている一方、READMEは現職等を再確認待ちとしている。取得済みと内容確認済みの意味が一致しているか再点検が必要。

### 3.5 地理・静的資産

- `public/data/states-10m.json` とビルド後の `dist/data/states-10m.json` を保持する。
- アプリ本体は地図データを `import.meta.env.BASE_URL + 'data/states-10m.json'` から取得する。
- JavaScript、CSS、地図データは静的成果物に含まれる。
- 一方、フォントはCSSのGoogle Fonts `@import` で閲覧時に外部取得するため、「閲覧時の外部通信なし」は厳密には成立していない。フォント取得に失敗してもシステムフォントへフォールバックするが、完全オフライン資産ではない。

## 4. 上院議員・候補者名が表示されない原因

### 上院議員名

1. `Seat` 型には `incumbent` があるが、生成時に100議席すべて `incumbent: null` としている。
2. 詳細画面は `incumbent ?? '基準日時点の確認未完了'` を表示するため、必ず確認未完了の文言になる。
3. `senate-list` 出典レコードは存在するが、各議席の `sourceIds` には使われておらず、氏名データも取り込まれていない。

### 候補者名

1. 全33通常選挙の候補者配列は、名前が `null` の未確認プレースホルダー1件だけである。
2. 州詳細の描画処理は州の2議席を列挙するだけで、`election.candidates` を走査・表示していない。
3. したがって、データへ候補者を追加するだけでも画面には出ず、データ確認と描画実装の両方が必要である。

## 5. 議席シミュレーションの計算方法

### 現在値

`currentCaucusCounts` は100件の `Seat.caucus` を単純加算する。現在データでは、圧縮行にある無所属2議席も自動的に民主党会派へ変換されるため、53 Republican / 47 Democraticになる。この会派付与は議席別の確認済み `caucus` データを読んだ結果ではなく、党派からの自動変換である。

### 仮定値

1. `elections.map(e => e.seatId)` を `Set` にし、「選挙対象の議席」を決める。
2. 100議席を1回ずつ走査する。
3. 非改選議席は現在の `Seat.caucus` をそのまま加算する。
4. 改選議席は `assumptions[seatId]` があればその会派、なければ現在の `Seat.caucus`（現保有会派維持の初期仮定）を加算する。
5. 変更された1議席は旧会派から1減り、新会派へ1増える。地図フィルターはこの処理に関与しない。

候補者名、情勢評価、勝率は計算に使わない。ユーザーは「候補者の勝者」ではなく「当選者が参加すると仮定する会派」を直接選ぶ。

### 多数派表示

- `unconfirmed > 0` なら「判定できません」。
- Democraticが51以上なら「民主党会派が51議席以上」。
- Republicanが51以上なら「共和党会派が51議席以上」。
- それ以外は「50対50：副大統領の決裁票により運営多数派が決まります」。

副大統領の所属・決裁票がデータ化されていないため、50対50時にどちらが運営多数派かまでは判定しない。

## 6. 通常選挙33議席と特別選挙2議席の扱い

### 通常選挙33議席

- Class IIの33議席から33件の通常選挙を生成している。
- ヘッダー、シミュレーション見出し、READMEも33件を前提にしている。
- テストは通常選挙が33件であることを検査している。
- 現在の33件は、党派内訳でR 19、D 13、I 1である。

### 特別選挙2議席

- リポジトリ内には `type: 'special'` の選挙レコードが1件もない。
- 依頼で示された2議席について、どの州・`seatId`・Class・対象任期かを示すデータも出典もない。この監査では推測で補わない。
- 地図本文には「★は特別選挙（確認済みデータに該当なし）」とあるが、★をSVGへ描く処理自体もない。
- 現在のシミュレーション対象は33通常議席だけであり、特別選挙2議席は合計・変更UI・多数派判定のすべてから欠落する。
- `simulatedCounts` は選挙の `seatId` をSet化するので、同じ議席に複数の選挙レコードがあっても集計上の議席自体は1回しか数えない。ただしUIは `elections.map(...)` で選挙レコードごとにselectを作るため、同一 `seatId` の通常・特別レコードを追加すると重複操作欄が生じる。
- テストには、特別選挙が2件あること、対象任期が正しいこと、特別選挙を追加しても100議席を超えないこと、同一議席の複数選挙を二重表示しないことの検証がない。

## 7. 未完成機能と仮データ

| 項目 | 現在の状態 | 判定 |
|---|---|---|
| 現職氏名 | 100議席すべて `null` | 未完成 |
| 候補者 | 33件すべて名前なしの未確認プレースホルダー | 仮データ／未完成 |
| 特別選挙 | 0件 | 未完成 |
| 情勢評価 | 全33件 `unavailable` | 未完成 |
| 激戦強調 | 判定対象がないため、ONにすると全州が薄くなる | UIは実装済み、実データ未完成 |
| 州別4文要約 | 50州すべて欠測説明 | 仮表示／未完成 |
| 出来事年表 | 0件 | 未完成 |
| 支援指標 | 型にもデータにも存在しない | 未完成 |
| 候補者表示 | 描画処理なし | 未完成 |
| 特別選挙の★ | 描画処理なし | 未完成 |
| PCホバー簡易情報 | SVGの`title`のみで、指定された簡易情報UIはない | 一部未完成 |
| 評価機関 | 未設定 | 未完成 |
| 静的地理資産 | 同梱済み | 実装済み |
| Google Fonts | 閲覧時外部通信 | 自己完結要件との不一致 |

「仮データ」と呼べるのは、名前なし候補者プレースホルダー、州別欠測文、未確認の任期文字列である。党派・会派データは画面で実数として使われているが、議席ごとの一次資料参照がなくREADMEが再照合を要求しているため、確認済み本番データとは扱えない。

## 8. 確認できた不具合・設計上の問題

### 高影響

1. **特別選挙2議席が完全に欠落**：地図、詳細、シミュレーション、検証の対象外。
2. **候補者描画がない**：候補者データを追加しても現UIに表示されない。
3. **無所属の会派を自動決定**：`R` 以外をすべてDemocraticへ割り当てており、「無所属を自動算入しない」というデータ方針と矛盾する。
4. **現職・党派等の出典追跡不足**：全議席がClass制度の出典だけを参照し、氏名は全件欠測。
5. **50対50の最終判定不足**：副大統領データがなく、どちらが運営多数派かを表示できない。

### 中影響

6. **同一議席に複数選挙を追加した場合のUI重複**：集計はSetで一議席化するが、操作UIは選挙件数分生成する。
7. **全評価未取得時の激戦フィルター**：ONで全改選州を含む全州が薄くなり、利用者には「激戦州がない」ように見える可能性がある。
8. **特別選挙記号の説明と実装が不一致**：説明はあるが描画処理がない。
9. **地図クリック後のフォーカス復帰が不安定**：州選択時に地図全体を再描画するため、保存したクリック元SVG要素はDOMから外れる。閉じる際にその古い要素へ `focus()` しても、実画面上の州へ戻らない可能性が高い。セレクトメニュー起点では要素が残る。
10. **外部フォント通信**：サイト本文は「閲覧時に外部サイトへ接続しない」と説明するが、CSSはGoogle Fontsへ接続する。
11. **出典が資料単位でない**：Census/BLS/BEAは入口ページで、州別の主張に使った表・対象年がない。

### テストで未確認

12. TopoJSON内の50州と州データの完全一致、全州の代替操作到達性、表示モードと凡例、特別選挙記号、欠測表示、キーボード操作、フォーカス復帰、レスポンシブの横幅は自動テストされていない。

## 9. テスト状況

### 自動テストで確認していること

`tests/data.test.ts` には3テストがある。

1. 50州、100議席、一意な `seatId`、各州2議席、州内Class重複なし、2026年通常選挙33件。
2. 現在会派合計が100。
3. 最初の改選議席を反対会派へ変更すると、片側が+1、元会派が-1。

`npm test` は監査時に成功した。`npm run build` も成功し、GitHub Pages用の `/us-midterms-2026/` を含む静的成果物を生成した。

### 自動テストで確認していないこと

- 特別選挙2件とその対象任期・出典。
- 選挙件数と「選挙対象の一意な議席数」の区別。
- 候補者・現職・評価・出典の必須項目。
- 無所属／空席／会派未確認の集計。
- 50対50時の副大統領による運営多数派。
- 全50州のプロフィール内容、4文構成、2〜4件の年表、source IDの参照整合性。
- 地図とデータのFIPS完全一致。
- DOM表示、Safari、iPad実機、キーボード、スクリーンリーダー、フォーカス復帰。
- GitHub Actions上での実行と実際のPages公開。

READMEにはPlaywrightブラウザー取得がHTTP 403で失敗し、E2Eと実機確認を行っていないことが記載されている。

## 10. GitHub Pagesへの公開構成

### 実装済み

- `vite.config.ts` の `base` は `/us-midterms-2026/`。
- `src/main.ts` は州境URLに `import.meta.env.BASE_URL` を使うため、Pagesのサブパスに追従する。
- workflowは `main` へのpushと手動 `workflow_dispatch` で起動する。
- Node.js 20で `npm ci` → `npm test` → `npm run build` を実行する。
- `actions/configure-pages@v5`、`actions/upload-pages-artifact@v3`、`actions/deploy-pages@v4` を使い、`dist` を `github-pages` environmentへ配置する。
- 必要権限は `contents: read`、`pages: write`、`id-token: write`。
- ビルド済み `dist/index.html` のJS/CSS URLにも `/us-midterms-2026/` が付いている。

### このリポジトリだけでは確認できないこと

- GitHub上でPagesのSourceが「GitHub Actions」に設定済みか。
- workflowがGitHub上で成功したか。
- 公開URL、リポジトリ所有者、実際のデフォルトブランチ。
- iPad Safariでの実機表示。

ローカルにGit remoteが設定されていないため、この監査環境から既存PR #1やGitHub Pagesの外部状態は確認できない。

## 11. 次に修正すべき項目（優先順位）

### P0：公開前に必須

1. **基準日時点の100議席を一次資料で再構築する**  
   現職名、党派、会派、空席、Class、任期を議席単位で登録し、各属性へ出典を紐付ける。無所属は自動で民主党会派にせず、確認値または未確認とする。
2. **特別選挙2議席を一次資料で確認して追加する**  
   州、`seatId`、Class、投票日、対象残任期、通常／特別の別、候補者、出典を登録する。通常33件、特別2件、一意な対象議席数を別々に表示・検証する。
3. **シミュレーションを特別選挙込みに修正する**  
   一意な議席単位で新議会を100議席に保ち、同一議席の複数選挙レコードを操作欄で重複させない。特別選挙の防衛・奪取も反映する。
4. **候補者データと候補者表示を実装する**  
   `candidates[]` を確認済み情報で埋め、氏名、党派、状態、会派方針、出典を州詳細へ表示する。未確定・候補なし・調査未完了を区別する。
5. **事実上の仮データを本番表示から分離する**  
   現在の党派・会派・取得日の根拠を再確認し、確認できない値は実数サマリーに使わない。

### P1：初版の内容要件

6. **統一機関の情勢評価を追加する**  
   原表記、表示分類、機関、評価日、URL、取得日を保持し、未取得と選挙なしを分ける。評価が全件未取得のときは激戦フィルターを無効化または説明する。
7. **50州の確認済み4文要約と出典を作成する**  
   地域差、産業と争点、過去の変化、今回との関係を州固有の資料で記述する。
8. **州別年表を2〜4件追加する**  
   出来事、地域への影響、観測された政治変化、因果解釈の確かさを分離する。
9. **副大統領と50対50判定をデータ化する**  
   基準日時点の副大統領と新議会での前提を明記し、判定不能な場合はその理由を表示する。
10. **出典を実際の表・記事単位へ細分化する**  
    組織入口ではなく、対象期間・公開日を持つ資料を各主張へ紐付ける。

### P2：品質・運用

11. **特別選挙の★と複数選挙表示を実装する**。
12. **外部Google Fontsを自己ホストまたはシステムフォントへ置換する**。
13. **地図クリック後のフォーカス復帰を修正する**。
14. **データ検証を拡張する**  
    特別選挙、100議席維持、source ID参照、FIPS地図結合、欠測、無所属・空席、プロフィール完成度をテストする。
15. **ブラウザーE2EとiPad Safari実機確認を行う**  
    390px前後、iPad縦横、キーボード、詳細開閉、モード、凡例、全州到達、シミュレーション、通信失敗を確認する。
16. **GitHub上でPages workflowと公開URLを確認する**  
    Pages設定、Actions成功、`/us-midterms-2026/`以下のJS/CSS/TopoJSON応答を確認する。

## 12. 監査で実行した確認

- `rg --files -g '!node_modules'` による収録ファイル一覧確認。
- `cat` / `sed` によるデータモデル、データ、描画、計算、テスト、Vite、workflow、READMEの確認。
- Pythonの読み取り専用スクリプトによる州数、党派数、会派生成結果、Class数、Class II州・党派の集計。
- `rg` による `special`、候補者、現職、出来事、外部URLの利用箇所確認。
- `npm test`、`npm run build`、`git diff --check`。

この監査では、選挙データを推測で追加・修正していない。ソースコード、データ、テスト、workflowにも変更を加えていない。追加したのは本監査文書 `CURRENT_STATE.md` のみである。
