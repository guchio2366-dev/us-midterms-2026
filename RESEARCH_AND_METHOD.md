# 調査・分類・計算方法

基礎データ基準日：2026-09-09（UTC）。研究モジュール更新日：2026-09-11（UTC）。この文書は、2026年米国中間選挙サイトで、原資料から公開画面まで情報をどう結び、何を計算せずに残すかを記録します。

## 1. 情報の種類を混ぜない

同じ画面に並ぶ情報でも、出所と確実性は同じではありません。本サイトは次の区分を維持します。

| 区分 | 意味 | 例 | 画面・データ上の扱い |
| --- | --- | --- | --- |
| 観測事実 | 原資料で直接確認した値・出来事 | 候補者名、調査結果、採決票、人口 | `EvidenceRef.kind: observed`。資料と根拠箇所を接続 |
| 外部評価 | 情勢機関による競争性判断 | Solid、Lean、Tilt、Toss Up | 機関、原表記、評価日を保持。勝率へ変換しない |
| 編集上の解釈 | 複数の事実を結ぶ読み方 | 調査間で先頭が異なる、論点との関係 | `interpretation` として事実から分離 |
| 仮説 | 今後検証する因果・影響の候補 | ある政策が投票選択へ与える影響 | `hypothesis` と明示し、確定予測に使わない |
| 利用者の仮定 | シミュレーションで選んだ将来結果 | OHを民主党会派と仮定 | 初期情勢や事実とは別の操作状態として表示 |

調査値、情勢評価、候補者の自己説明、編集上の推論を一つの「予測値」へ合成しません。現在の実装は候補者別当選確率も算出しません。

## 2. Source、EvidenceRef、公開状態

研究データは次の順で原資料へ遡れるようにします。

1. `Source`：資料名、発行者、直接URL、公開・更新日、対象期間、取得日、内容確認日。
2. `EvidenceRef`：表、設問、段落、採決行など、実際に根拠とした箇所。`observed` / `interpretation` / `hypothesis` もここで指定。
3. Poll、RatingObservation、HistoricalResult、RaceBrief、CandidateBrief、IssueReport、RollCallVote、News等：該当する `sourceIds` と `evidenceIds` を保持。

公開状態は全研究レコードで共通です。

| 状態 | 用途 | 公開画面 |
| --- | --- | --- |
| `draft` | 入力・照合中 | 非表示 |
| `reviewed` | 内容確認済みだが公開待ち | 非表示 |
| `published` | 公開可能 | 表示 |
| `withdrawn` | 取り下げ | 非表示 |

`src/research-logic.ts` の公開セレクタは `published` だけを返します。ニュースは公開状態で絞り込んでから更新順に並べ、10件ずつページングします。現在は公開12件と回帰検証用の非公開下書き1件があり、画面では1ページ目10件、2ページ目2件です。下書きは総件数にもページ数にも入りません。

公開前の構造検証では、重複ID、存在しないSource・EvidenceRef・選挙・候補者・州・論点・権限、別選挙に属する候補者をPollへ誤接続するケース、無効な過去得票、ニュースの置換先などを検出します。

## 3. 安定IDと候補者名簿

選挙、議席、候補者、人物、論点、資料を表示名ではなくIDで接続します。

- `seatId`：上院の議席。例：`OH-3`。
- `electionId`：選挙年・州・Class・通常／特別を含む選挙。例：`2026-OH-3-special`。
- `candidateId`：特定選挙の候補者レコードをPoll、CandidateBrief、Newsへ接続するキー。
- `personId`：同一人物を別用途で識別するキー。選挙内の結合キーとは分離。

候補者名や出典ページが更新されても、同じ候補者レコードなら `candidateId` を維持します。同姓候補を名前だけで統合しません。Alaskaの現職Dan S. SullivanとDaniel J. Sullivan Jr.は、別ID、別人物、別候補カードです。

`ballotStage` は次の三つを区別します。

- `general-ballot`：本選投票用紙に印刷される候補。
- `primary-ballot`：予備選段階で、本選候補として未確定。
- `write-in`：宣言済み書き込み候補。印刷候補数へ混ぜない。

現行名簿は122人で、`general-ballot` 100人、`primary-ballot` 10人、`write-in` 12人です。画面上の「印刷」は実装上write-in以外の110人を指しますが、予備選未確定州の `primary-ballot` は本選掲載済みとは扱わず、`contestStatus` と合わせて表示します。Maineは本選印刷候補2人・宣言済みwrite-in 6人、Ohio特別選挙は本選印刷候補4人・宣言済みwrite-in 3人です。

上院はClass IIの通常33選挙に、Florida Class IIIとOhio Class IIIの特別選挙を加えた35議席です。同一議席を重複計上しないよう、2027年議会の100議席は `seatId` 単位で集計します。基礎名簿の基準日時点ではDelawareは9月15日の予備選前、Rhode Islandは9月9日の結果確定前で、予備選候補を本選候補へ昇格させていません。

## 4. 重点6州の第一波

全35選挙の基礎名簿と統一情勢に加え、第一波はAlaska、Iowa、Maine、Michigan、Ohio、Texasを同じ研究契約で深掘りします。

| 州 | 対象選挙 | 実装した主な論点 |
| --- | --- | --- |
| Alaska | Class II通常選挙 | 第1選択とRCV最終ラウンドを分離。CookとInside Electionsの履歴、同姓候補を区別 |
| Iowa | Class II通常選挙 | YouGovのRV・loose LV・strict LVを同一studyに束ね、Emerson・Suffolkと別々に比較 |
| Maine | Class II通常選挙 | 民主党候補交代、順位選択投票、印刷候補2人と宣言済み書き込み候補6人を分離 |
| Michigan | Class II通常選挙 | 全6印刷候補、直近3調査の小差、主要3評価機関のToss Upを併記 |
| Ohio | Class III特別選挙 | JD Vanceの副大統領就任による欠員、印刷候補4人・書き込み候補3人、評価変更 |
| Texas | Class II通常選挙 | 初回候補者選択と未定者のleaner割当後を、同じ調査の別段階として表示 |

各重点選挙には `completeness: substantial` のRaceBriefを置きます。RaceBriefは均衡度、主要論点、根拠付き分析、支持変化について確認できないこと、投票参加上の注意、更新条件、関連Poll・Rating・Issueを保持します。

各州の主要2候補には公開CandidateBriefがあります。Iowaの第三候補とAlaskaの同姓候補にも、表示上の混同を避けるため追加Briefがあります。CandidateBriefでは候補者・陣営資料、公式経歴、採決、FEC等を分け、現在の立場、実績、支持・資金、相手との違い、注視政策、反対政策を表示します。陣営資料の主張を第三者が確認した効果とは扱いません。

現行の公開研究データは、Poll 21レコード（`studyId` 単位で17調査）、RatingObservation 25観測、`substantial` RaceBrief 6件、CandidateBrief 14件、重点6州のHistoricalResult 6件です。IssueReportは8件、RollCallVoteは2件で、根拠箇所はEvidenceRef 93件に分けています。

## 5. 投票調査を縮約しない

Pollは見出しの二候補数字だけでなく、実施期間、母集団、標本数、方法、スポンサー、設問、精度表記、回答区分、原資料の完全性を保持します。

| フィールド | 意味 |
| --- | --- |
| `pollId` | 一つの表示可能な結果集合 |
| `studyId` | 同じ標本・同じ調査から生じた複数の結果集合を束ねるID |
| `population` / `populationLabel` | LV、RV、adult、general voters、subgroupと原資料上の対象 |
| `resultStage` | `base`、leaner追質問・割当後、第1選択、脱落過程、計算上の直接対決、最終ラウンド等 |
| `conditionLabel` | 「strict LV」「未定者のleaner回答を割当後」「最終RCVラウンド」などの表示条件 |
| `questionExact` | 設問が原資料の文面どおりか、要約かを区別 |
| `completeness` | 回答内訳・方法を完全に確認できない場合を `partial` として明示 |
| `residualTreatment` | 合計差を `unreported`、`rounding`、`none` に分ける |

同一 `studyId` の別母集団・別段階は同じ囲みで表示します。直近3studyを先に出し、それ以前を折りたたみます。例えばTexasのOverton調査は初回44.0対43.4と、未定者のleanerを割り当てた50対50を別結果として保持します。Alaskaの第1選択とRCV最終ラウンドも直接比較可能な同じ段階とは扱いません。

`completeness: partial` は、アクセスできた資料だけでは回答内訳、初回ラウンド、設問原文、または方法の一部を確認できないことを示します。`partial` がないことだけで、原資料の全項目を網羅したと認定するものではありません。

`residualTreatment: unreported` で公表値の合計が99.99%未満の場合だけ、100%との差を「内訳未掲載」として灰色表示します。`rounding` は99%、100.1%、101%等の差を丸めとして扱い、架空の回答区分を足しません。`none` はサイト側で残余を推定・追加しません。異なる母集団・方法・設問を混ぜた平均、二つの横断調査からの支持移動、候補者別当選確率は算出しません。

## 6. 情勢評価の比較

全35選挙の地図色は、比較可能性を保つためSabato's Crystal Ballの2026-08-26評価に統一しています。原表記 `Safe` を画面上の `Solid`、`Tossup` を `Toss Up` に正規化しますが、原表記も保存します。

重点州ではCook Political Report、Inside Elections、Sabato、Fox等のRatingObservationを追加します。同じ機関の変更だけを日付順の履歴としてつなぎ、異なる機関は同日時点の比較として並べます。機関横断の平均や独自の合成分類は作りません。重点州の比較欄が変わっても、統一基準を採る地図色を一州だけ自動変更しません。

現在の25観測はCook 11、Inside Elections 10、Sabato 3、Fox News Power Rankings 1です。`ratingRaw` に機関の原表記、`category` に表示用分類を保持します。比較データのTiltは地図の7分類へ無理に置き換えません。

## 7. 2024年大統領選の横棒

重点6州はFECの候補者別確定票をHistoricalResultとして保持します。民主党候補票と共和党候補票の合計を分母にし、二大政党票の割合を生票から計算します。第三候補票を消去した「全有効票比」ではなく、二候補間の均衡度を見る正規化表示です。

他44州は既存の確認済み二候補差データから二大政党比を表示するため、重点州のような候補者別生票レコードではありません。サイト内で見た目が同じでも、出典粒度の違いはこの文書に残します。

## 8. ニュースの選定と関連付け

ResearchNewsItemは、種類、出来事の日付と精度、公開・更新日、概略、考えうる影響、その影響が観測・解釈・仮説のどれか、政策段階、採用理由、更新内容、位置、関連選挙・候補者・論点、出典を保持します。

現在は13レコード中12件が `published`、1件が検証用 `draft` です。先に公開状態で絞り、`updatedAt` 降順、同日なら `publishedAt` 降順、さらに同じなら `newsId` 順に並べてから10件ずつ分割します。このため公開画面は10件＋2件の2ページで、下書きは件数やページ境界へ入りません。これは発生日順ではありません。

ニュース詳細から関連論点・州へ移動できます。州の選挙カードには `relatedElectionIds` が一致する公開ニュースを最大4件、論点レポートには `issueIds` が一致する公開ニュースを最大5件表示します。ニュースから論点へ進んだ場合はパネル内履歴、州へ進んだ場合は州詳細の「ニュースに戻る」で復帰します。`relatedCandidateIds` は現在、候補者名の表示に使い、候補者カードへの独立リンクにはしていません。場所が意味を持つ記事は州・地点を地図表示し、全国発言など地理的な地点を置く意味がない記事は地図を省きます。訂正で旧記事を置き換える場合は `supersedesId` を使いますが、別調査や別母集団を置換扱いにしません。

現在の公開12件は重点州の投票調査、候補交代、情勢評価変更が中心です。重要な大統領発言、政策実施、デモ、社会運動を常時監視するライブニュース集合ではありません。

## 9. 議会権限と票数

票数は「全議席が埋まり全員出席」という説明用の名目値と、憲法・院規則上の実際の基準を分けます。

| 権限 | 下院 | 上院 | 実際の基準に関する注意 |
| --- | --- | --- | --- |
| 通常立法 | 同一文面を可決 | 同一文面を可決 | 通常は各院の出席投票者過半数。上院クロージャーは原則60 |
| 歳入・歳出 | 歳入法案の起点、歳出可決 | 修正・可決 | 通常は両院過半数。上院で60票が必要になる場合がある |
| 監督 | 委員会調査・召喚 | 委員会調査・召喚 | 委員会規則に従い、多数党が委員長・議題を握る |
| 指名 | 原則関与なし | 助言と同意 | 出席投票者過半数。討論終結も単純多数 |
| 条約 | 実施法・歳出では関与 | 助言と同意 | 出席上院議員の3分の2 |
| 弾劾 | 過半数で訴追 | 裁判 | 有罪・罷免は出席上院議員の3分の2 |
| 拒否権再可決 | 3分の2 | 3分の2 | 全員出席なら下院290、上院67 |
| 財政調整 | 過半数 | 対象範囲なら単純多数 | 上院60票を要しないがバード・ルール等の制約あり |

反対党が一院だけを取れば新法・歳出・議題を強く制約できますが、既存法に基づく大統領権限が自動的に消えるわけではありません。下院多数だけなら弾劾訴追は可能でも罷免できず、両院多数でも拒否権突破には各院3分の2が必要です。

RollCallVoteは抽象的な票数説明と実際の議員行動をつなぐ例です。現在はS.J.Res.37とH.R.1の上院採決を収録しています。多数派を構成する一議席と、個別法案でその議員が賛成するかは別問題として表示します。

## 10. 8論点と事例

政策の主な対象で一次分類し、権限、州、候補者、影響指標は関連IDで横断させます。

1. 家計・税・財政
2. 貿易・産業・農業
3. 移民・国境
4. 医療・家族・教育
5. エネルギー・気候・環境
6. 外交・安全保障
7. 法の支配・権利・選挙制度
8. 地域基盤・住宅・公共サービス

分類規則は「最も直接に政策対象となる領域へ一つだけ置く」です。例えば中国関税による大豆価格は一次分類を「貿易・産業・農業」とし、家計、外交、地域は関連軸にします。

全8枠は `published` ですが、本文は `completeness: partial` です。重点州の州別事例を追加した段階であり、全国有権者の因果関係を確定するレポートではありません。新しい事例は州・選挙・Source・EvidenceRefへ接続して差し替えます。

## 11. 大豆・中国ケース

大豆ケースはUSDA NASSの2026年8月州別生産予測と、USDA ERSの2025/26年度10月〜7月輸出表を使います。

- 中国向け：12,356,115トン
- 前年同期：中国向け21,598,375.9トン
- 前年同期比：−42.8%
- 世界向け：36,805,330.8トン
- 中国比率：33.6%

州の生産量は通商政策への経済的な曝露を示しますが、賛否や投票理由を示しません。生産上位州、2024年の政治的基準点、2026年選挙の有無と情勢、候補者の関税・市場開放・農家支援方針の順に確認します。選挙後も生産量だけから因果を断定せず、出口調査、地域別得票、所得、価格等を追加照合する必要があります。

## 12. 50州プロフィールと下院435区

州詳細は比較可能な背景指標を表形式にし、2024年二大政党票を横棒で示します。

- Census：2025年人口と2020年基準からの変化。
- FEC：2024年大統領選。
- BEA SAGDP2：2025年の最大民間2桁NAICS産業と民間GDP比。
- USDA NASS：該当州で選挙を読むうえで意味がある場合の特筆指標。
- 上院データ：2026年選挙、候補者確定状態、統一情勢。

定量指標は投票理由の説明文へ自動変換しません。文章はRaceBriefがある重点州の選挙情勢と、その根拠へ限定します。

下院の現議会総数はOffice of the Clerkの2026-09-04 XML、2026年選挙は270toWinの2026-08-31合意情勢と2026年用TopoJSONを使います。中間期の区割り変更があるため、2024年境界ではなく `election: 2026` の435形状だけを用い、地区データとの完全一致をテストします。

評価内訳はSolid D 182、Likely D 14、Lean D 12、Toss Up 20、Lean R 10、Likely R 30、Solid R 167です。機械的な初期仮定はD208・R207・未確定20で、Solid以外86区を変更できます。常に合計435を保ちます。情勢表の参照現職・党派は、現議会の公式構成を再計算する用途には使いません。

## 13. 差し替え可能なデータ設計

研究本文を画面ロジックへ直書きせず、役割ごとのモジュールへ分けます。

| ファイル | 役割 |
| --- | --- |
| `src/data/research-model.ts` | Poll、RatingObservation、RaceBrief、CandidateBrief、IssueReport、RollCallVote、News等の型 |
| `src/data/research.ts` | Iowa・Michiganを含む共有研究、HistoricalResult、8論点、採決例 |
| `src/data/research-focus.ts` | Maine・Ohio・Texas・Alaskaを中心とする第一波の追加研究 |
| `src/data/research-sources.ts` / `src/data/research-focus-sources.ts` | SourceとEvidenceRef |
| `src/data/news.ts` | ニュース本文、公開状態、関連ID、位置 |
| `src/data/senate-races.ts` | 候補者、candidateId、ballotStage、contestStatus、地図基準情勢 |
| `src/research-logic.ts` | 公開フィルター、並び順、study、同一機関の評価履歴、参照検証 |
| `src/ui/research.ts` | 研究レコードを表示するマークアップ関数 |

本文や出典を差し替えても、`newsId`、`electionId`、`candidateId`、`pollId`、`ratingId`、`issueId`、`sourceId`、`evidenceId` を結合キーとしてUIを維持します。同じ調査の別条件は同じstudyIdの別pollIdにし、別調査を上書きしません。候補者名簿の変更時は、PollResult、CandidateBrief、Newsの関連候補ID、`ballotStage`、`contestStatus`、`candidateResearchStatus` を一緒に検査します。

これはCMSや実行時APIではありません。更新にはTypeScriptデータの変更、再検証、静的ビルド、再配備が必要です。

## 14. 更新・検証・公開手順

1. 原資料をSourceへ登録し、内容確認日まで埋める。
2. 実際に使う表・設問・段落をEvidenceRefへ登録する。
3. 既存の安定IDを確認し、研究レコードを `draft` で追加する。
4. 同じ調査の別母集団・別段階なら共通 `studyId` と適切な `resultStage` / `conditionLabel` を付ける。
5. Poll、Brief、NewsのSource・Evidence、選挙、候補者、州、論点、権限を接続する。
6. `npm test` と `npx tsc --noEmit` を実行し、原資料と画面表示を再照合する。
7. `reviewed` を経て `published` に変更する。未確認部分があれば `partial` とNotesを残す。
8. `npm run build` で静的成果物を生成する。
9. `main` 更新後、GitHub Actionsの `npm ci` → `npm test` → `npm run build` → Pages配備を確認する。

テスト件数は実装に合わせて増減するため、この文書では固定しません。テストは基礎データの総数、重点6州のRaceBrief・CandidateBrief、安定ID、別選挙候補の混入、Maine・Ohioの印刷／書き込み候補、Texas・AlaskaのPoll段階、残余回答の扱い、公開ニュースと10件ページング、関連ID、2024年生票、Rating履歴、下院435形状などを検査します。

ローカルでの標準コマンドは次の通りです。

```bash
npm ci
npm test -- --reporter=verbose
npx tsc --noEmit
git diff --check
npm run build
npm run preview
```

## 15. 主な資料群

- U.S. Senate：現職、Class、会派、議会権限、クロージャー、条約、採決原票。
- U.S. House / Office of the Clerk：下院権限、現議会の議席構成。
- Constitution Annotated（Library of Congress）、Congressional Research Service：立法、拒否権、指名、条約、弾劾、監督、財政調整。
- 各州選挙当局・州政党：2026年候補者、投票用紙、予備選状態。
- Sabato's Crystal Ball：上院地図の統一情勢。
- Cook Political Report、Inside Elections、Fox等：重点州の比較用評価。
- 各調査会社の原表・方法資料：母集団、設問、結果、精度、集計段階。
- FEC：2024年大統領選の候補者別確定票、候補者資金。
- Census、BEA、USDA NASS / ERS、USTR：州背景と農業・通商。
- 270toWin、NCSL：下院合意情勢、2026年区割り、区割り変更。

## 16. 現時点の制約

- 実質的なRaceBrief、Poll、CandidateBrief、評価履歴は重点6州が第一波です。他州は同じ深さの調査済みではありません。
- 収録Pollは原表を確認できた選択集合であり、全調査の網羅、平均、トレンドモデル、勝率ではありません。
- 2024年候補者別生票のHistoricalResultは重点6州のみです。他州は既存の二候補差データを使います。
- PolicyPositionの型と差し替え口はありますが、構造化配列はまだ空です。候補者方針はCandidateBrief内にあります。
- 8論点はすべて部分公開で、州事例から全国的因果を断定しません。
- 公開ニュースはライブ監視や網羅的なニュースサービスではありません。大統領発言、政策実施、デモ等は原資料確認後に追加します。
- 現在の公開12件はすべて選挙関連で、`policyStage` は `not-applicable` です。ほかのNewsKindと政策段階は今後の差し替え口です。
- `supersedesId` は参照整合性を検査しますが、旧記事の自動非表示や新記事への自動転送は行いません。
- 関連ニュースの移動は画面内状態で、共有可能なURLや再読み込み後の復元には対応していません。
- 権限から境界州を自動選択する経路、因果係数、当選確率、Xからの自動話題抽出は未実装です。
- 候補者、予備選結果、情勢、区割りは変化します。DE・RIを含む更新では候補者段階、選挙状態、関連ID、出典を同時に再検証します。
- 実際の票数要件は欠員、出席、棄権、院規則、法案種別で変わります。218、51、60、67、290は説明用の基準です。
- 特別選挙当選者の具体的な就任日は未定で、残任期の終了日とは分けて表示します。
- 自動テストはデータと純粋ロジックが中心です。ブラウザE2E、視覚回帰、スクリーンリーダー操作、lint、coverageの専用スクリプトはありません。
